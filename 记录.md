# 本次本地部署修复记录

## 当前结果

- 已修复启动脚本，服务当前以容器 `pwn-json-stack-local-test` 运行。
- 监听端口：`127.0.0.1:8000`，宿主机映射为 `0.0.0.0:8000->8000/tcp`。
- 实测发送一条错误指令后，服务返回：`b'\t\x00\x00\x00"bad cmd"'`，说明容器和题目程序链路已经打通。

## 我遇到的问题与处理

### 1. 题目启动模型写错，导致容器秒退

现象：

- 原来的 `build/src/start.sh` 先在 chroot 内后台启动 `/pwn`，然后轮询 `/proc/net/tcp`，假设题目会自己监听 `6666`。
- 实际运行时容器很快退出，`docker ps -a` 显示 `Exited (1)`，`8000` 端口也无法连接。

定位：

- 直接运行 `/usr/sbin/chroot --userspec=ctf:ctf /home/ctf /pwn` 时，程序会立即以 `0` 退出，不会常驻监听。
- 反汇编 `main` 后确认：程序先从 `stdin` 读取 4 字节长度，再读取 JSON 数据，处理完一次请求后直接退出。
- 这说明它是“单次 stdin/stdout 服务”，不是“自带 socket 的守护进程”。

解决：

- 修改 `pwn-json_stack/build/src/start.sh`。
- 去掉“后台启动 `/pwn` + 检查 6666 端口 + 转发到 6666”的整段逻辑。
- 改成由 `socat` 直接监听 `8000`，并在每次有连接时执行一次：`/usr/sbin/chroot --userspec=ctf /home/ctf /pwn`。

当前核心启动方式：

```sh
exec socat -T60 TCP-LISTEN:8000,reuseaddr,fork EXEC:'/usr/sbin/chroot --userspec=ctf /home/ctf /pwn',stderr
```

### 2. `socat` 首次写法因为 `ctf:ctf` 解析失败

现象：

- 首轮修复时我写成了 `--userspec=ctf:ctf`。
- 容器虽然不再秒退，但日志报错：

```text
socat[9] E "EXEC:/usr/sbin/chroot --userspec=ctf": wrong number of parameters (2 instead of 1)
```

原因：

- `socat` 地址语法会把冒号当成分隔符，`ctf:ctf` 在这里被错误拆开了。

解决：

- 改成 `--userspec=ctf`，让 `chroot` 使用该用户默认主组。
- 修改后，服务可以正常按连接拉起题目进程并返回结果。

### 3. Docker Hub 元数据请求偶发 EOF，导致后续 rebuild 不稳定

现象：

- 第一次 `docker build` 是成功的。
- 后续再次 `docker build` 时，Docker 在解析 `FROM ubuntu:22.04` 这一步请求 Docker Hub 元数据，出现：

```text
failed to do request: Head "https://registry-1.docker.io/v2/library/ubuntu/manifests/22.04": EOF
```

说明：

- 这是外部网络/registry 访问抖动，不是题目代码本身的问题。
- 因为本机当时没有单独缓存 `ubuntu:22.04` 这个基础镜像，所以重新构建会受这个问题影响。

解决：

- 代码层面已经把 `start.sh` 修好。
- 为了先把环境跑起来，我复用了本地之前已经构建成功的镜像 `pwn-json-stack-local`。
- 然后把修好的 `start.sh` 拷进去，提交成新镜像：`pwn-json-stack-local-fixed`。
- 最终使用这个本地镜像成功启动容器。

## 当前运行的容器与镜像

- 运行中的容器名：`pwn-json-stack-local-test`
- 当前使用镜像：`pwn-json-stack-local-fixed`

## 我执行过的关键验证

查看容器状态：

```sh
docker ps --filter name=pwn-json-stack-local-test --format '{{.Names}} {{.Status}} {{.Ports}}'
```

实际输出结果对应的状态是：容器 `Up`，并且 `8000` 端口已映射。

发送一次最小测试请求：

```sh
python3 -c 'import socket,struct; payload=b"{\"data\":\"nope\"}"; s=socket.create_connection(("127.0.0.1",8000),5); s.sendall(struct.pack("<I", len(payload))+payload); print(s.recv(4096))'
```

返回：

```text
b'\t\x00\x00\x00"bad cmd"'
```

这说明：

- `docker` 已经起来了。
- `socat` 在监听 `8000`。
- 它能够为连接拉起 chroot 内的 `/pwn`。
- `/pwn` 能收到输入并正常返回响应。

## 后续建议

- 如果后面要重新从源码完整 `docker build`，需要先确认本机能稳定访问 Docker Hub，或者先手工 `docker pull ubuntu:22.04` 成功。
- 代码修复已经落在源码文件 `pwn-json_stack/build/src/start.sh`，等网络恢复后重新 build 也会得到同样的启动逻辑。
