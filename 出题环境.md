# 出题环境心得

## 目标优先级

这类题目的优先级不要写反。

1. 先保证出题平台能稳定拉起。
2. 再保证 GitHub Actions 能稳定产出镜像和附件。
3. 最后才是本地调试体验。

本地能通，不代表平台能通。平台能通，才算题目真的可交付。

## 这次项目里踩过的坑

### 1. 不要假设平台宿主机环境很完整

最开始把运行链默认建立在 `tmux` 上，这对本地调试没问题，但对平台不稳。

更稳的做法是：

- 用 `pid` 文件判断 QEMU 是否还活着
- 用 `FIFO + console.log` 代替 `tmux capture-pane`
- 用容器内脚本自己完成启动、等待、清理

结论：平台依赖越少越好，尤其不要默认平台宿主机有 `tmux`。

### 2. GitHub Actions 里的容器和本地主机不是同一个世界

本地主机装了完整的 QEMU 资源，不代表容器里的 QEMU 也完整。

这次真实踩中的点有两个：

- `pcnet` 启动时缺少 `efi-pcnet.rom`
- 默认 VGA 设备缺少 `vgabios-cirrus.bin`

修复思路：

- 显式使用 `-netdev` + `-device pcnet,romfile=`
- 显式加 `-vga none`

结论：QEMU 一旦进入容器，必须把显卡、网卡这类默认设备依赖显式写死，不能靠“系统默认值应该没问题”。

### 3. 固件 rootfs 的执行位不能想当然

固件目录虽然在本地看起来可执行，但进 Git、进 Docker build context、再进镜像之后，执行位可能被抹掉。

这次就出现了：

```text
chroot: failed to run command `/bin/sh': Permission denied
```

根因不是 `chroot` 本身坏了，而是固件里的 `/bin/busybox`、`/bin/upnp`、`/etc/profile` 这类文件在镜像里变成了 `0644`。

结论：

- 不要只看本地文件权限
- 要看 `git ls-files --stage`
- 要看真正进镜像后的权限
- 对固件树要在构建阶段主动修正关键执行位

这次最终的处理方式不是只修一个文件，而是在构建 rootfs 镜像前统一补关键目录和脚本的执行位，至少包括：

- `bin`
- `sbin`
- `lib`
- `usr/bin`
- `usr/sbin`
- `usr/lib`
- `etc/init.d`
- `/etc/profile`

否则本地看起来像“文件都在”，实际一进容器再 `chroot` 就直接死掉。

### 4. 固件挂载后不一定自带 `/tmp`

这次还有一个非常阴的坑，不是服务没起来，而是初始化脚本一开始就写日志失败了：

```text
/bin/sh: cannot create /tmp/router-init.log: nonexistent directory
```

表面现象是 workflow 一直在刷：

```text
[*] waiting for 37215, current http_code=000
```

但真正原因不是端口探测有问题，而是挂载出来的固件根文件系统里根本没有 `/tmp`，导致 `/etc/profile` 里的后台初始化链根本没跑起来。

结论：

- 不要默认固件 rootfs 自带标准目录
- 在挂载点启动前主动 `mkdir -p tmp`
- 同时补 `chmod 1777 tmp`

### 5. 平台链路要做黑盒 smoke test

只做 `docker build` 不够，要做：

1. 构建镜像
2. 推送包
3. 用刚构建出的镜像拉起容器
4. 等端口 ready
5. 用 PoC 跑一遍
6. 失败时上传日志

否则 workflow 看起来绿了，实际上平台一跑就死。

这里还有一个补充：smoke test 的发起位置也很重要。

不要默认：

```bash
docker exec <challenge-container> python3 exp.py --host 127.0.0.1 --port 37215
```

就等价于“外部选手访问题目端口”。

更稳的做法是让第二个独立进程或者第二个容器，从容器外部去打已经发布出来的端口。这样测到的才是更接近平台真实访问路径的结果。

### 6. flag 回传链路也会有“看起来利用成功但用户拿不到结果”的假成功

这次 PoC 打过去以后，UPnP 接口其实已经执行成功了，`flag_out` 也真的生成了，但用户侧依然会遇到空响应或者像断开连接一样的体验。

根因在于之前用了：

```sh
cat flag_out | nc -l -p "$PORT"
```

这个写法在这里不稳，`cat` 会先把输入吃完，`nc` 监听建立后客户端拿不到完整内容，最后表现成“看起来 exploit 成功了，但没有 flag”。

最终换成：

```sh
nc -l -p "$PORT" < flag_out
```

这才是稳定的单次回传方式。

结论：只看到目标侧副作用还不够，必须确认外部选手视角能真的收到 flag。

后面又踩到一个更隐蔽的点：即使 relay 方式对了，如果只在后台脚本里“等 5 秒端口释放”，然后立刻 `nc -l`，在 GitHub Actions 这种慢环境里也可能失败。

因为 `upnp` 在 exploit 后不一定 5 秒内就完全退出；这时 `nc` 抢占同一个端口会直接失败，然后脚本如果不重试，就会出现：

- exploit 端看到 `UpgradeResponse`
- 之后一直拿不到 flag
- `exp.py` 最终打印 `flag not ready`

最终要改成：

- 持续 `killall upnp` 和 `killall mic`
- 持续检查 `37215` 是否真的释放
- `nc -l` 失败时继续重试，而不是失败一次就结束

但这条链在 CI 和平台环境里本质上还是偏脆，因为它依赖：

- 固件里的服务确实按预期退出
- 同一端口能被及时抢占回来
- guest 内工具链足够完整

所以这次最终稳定方案又往前走了一步：

- 对外仍然只开放 `37215`
- QEMU 内部把 guest 的 UPnP 服务转发到容器内的另一个端口
- 容器侧单独起一个 host relay，统一监听对外 `37215`
- 普通 HTTP 请求由 host relay 代理给 guest
- exploit 成功后，guest 通过 BusyBox `wget` 把 flag 回连到容器内部 callback 端口
- 后续选手再连 `37215` 时，由 host relay 直接把 flag 原样返回

这样就不再依赖 guest 内去抢同一个对外端口。

### 7. GHCR 只是分发手段，不是平台前提

这类题目的可交付方式至少要准备两套：

- GHCR 镜像
- 平台直传附件

这次最后保留的产物是：

- 镜像包：`cve2017iot-hg532-image.tar.gz`
- 平台直传包：`cve2017iot-platform-direct.tar.gz`

结论：不要把部署能力单点绑定到 GHCR。

### 8. WSL 代理问题和 Docker 网络问题要分开看

PowerShell 能走代理，不代表 WSL 能走。
WSL 能访问 GitHub，不代表 Docker daemon 也能访问 Docker Hub。

排查时必须分层：

- 当前 shell 环境变量
- WSL 到代理端口是否真的通
- Windows 代理监听是否真的活着
- Docker daemon 是否也能走这条链

结论：看到 `127.0.0.1:7897` 不等于代理就真的可用。

### 9. 不要假设固件里的 BusyBox applet 是完整的

这次还有一个很隐蔽的假成功：

- PoC 打过去返回了 `200 UpgradeResponse`
- guest 里的副作用文件也确实生成了
- 但 `flag_out` 一直是空的，所以外部始终拿不到 flag

最后不是 relay 的问题，而是 helper 脚本里用了固件里并不存在的命令：

- `tr`
- `printf`
- `sync`

在这套 rootfs 里，这几个 applet 没有可用链接，所以脚本虽然被执行了，但中途直接报错。

修复方式：

- `check_cache.sh` 里去掉 `tr -d '\r\n'`，直接 `cat`
- `report_flag.sh` 里把 `printf` 改成 `/bin/echo`
- `report_flag.sh` 里把 `sync` 改成 `/bin/busybox sync`

结论：写固件内 helper 脚本时，不要拿宿主机或完整 Linux 发行版的命令集去想当然，必须按固件里真实存在的工具来写。

## 当前题目的稳定交付原则

### 端口

- 题目实例只暴露 `37215/tcp`

### flag

- 真实 flag 不进仓库
- 运行时通过 `FLAG_VALUE` 注入

### 优先部署方式

1. 平台支持上传目录并构建 `Dockerfile`
2. 平台支持导入镜像 tar
3. 平台最后才考虑直接拉 GHCR

## 这次最终修好的关键点

- `start.sh` 里显式关闭默认 VGA ROM 依赖，补成 `-vga none`
- `start.sh` 里显式使用 `-netdev` 和 `-device pcnet,romfile=`
- `build_router_rootfs_image.sh` 里在打包前恢复关键目录和脚本执行位
- `init_router_console.sh` 里默认回到 console bootstrap，不依赖 SSH
- `init_router_console.sh` 里给路由启动留出更长等待时间
- `init_router_console.sh` 里补建挂载 rootfs 下的 `/tmp`
- `init_router_console.sh` 里把 flag relay 改成 `nc -l ... < flag_out`
- `ctf/guest/check_cache.sh` 里不再依赖缺失的 `tr`
- `ctf/guest/report_flag.sh` 里改成只用固件中可用的 `/bin/echo` 和 `/bin/busybox sync`

## 每次交付前的最小检查表

1. 本地脚本能启动 QEMU 并看到 `37215`
2. 本地 PoC 能直接回显 flag
3. GitHub Actions 不只是 build 成功，smoke test 也要成功
4. `Packages` 里确实出现镜像
5. `Artifacts` 里确实有镜像包和平台直传包
6. 平台环境只开一个端口时，仍能正常拿 flag

## 最终验证结果

这次最后一次本地黑盒验证已经通过，验证命令是：

```bash
python3 exp.py --host 127.0.0.1 --port 37215 --yes --ready-timeout 20 --flag-timeout 20
```

结果链路是：

- 外部端口 ready
- PoC 收到正常 `UpgradeResponse`
- 最终直接回显本地注入测试 flag

这说明现在修好的不是“目标侧副作用”，而是选手视角从打 PoC 到拿 flag 的整条链路。

## 对以后自己的提醒

- 不要把“本地能跑”误认为“题目完成”
- 不要默认平台有 `tmux`
- 不要默认容器里的 QEMU 带齐所有 ROM
- 不要默认固件文件执行位在 Git 和 Docker 之后还保持正确
- 不要默认挂载后的固件 rootfs 自带 `/tmp`
- 不要把“PoC 执行成功”误认为“flag 回传成功”
- 不要把 GHCR 当成唯一交付路径
- 出题题目要以平台稳定性优先，而不是以本地调试舒适度优先
